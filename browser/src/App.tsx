import React from "react";

import { ApolloWrapper } from "src/ApolloWrapper";

import { Index } from "src/component";

export const App = () => {
  return (
    <ApolloWrapper>
      <Index />
    </ApolloWrapper>
  );
};
